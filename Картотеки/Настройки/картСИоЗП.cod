Class inherited ТББ_Базовый.Базовые.картДокумент "Настройки расчета формы СИоЗП";

inclass public

Import ТББ_Базовый Classes Переменные;

--{{2_ Свойства

inclass private
  var КлассИнтерфейса: class МашинаРеквизитов.Базовые.интЗапись = Настройки.интСИоЗП;
  var ПолеСтолбцаНаше: String:= 'НашеУчреждение.Имя';
  var ИдентифицироватьНашеПриСоздании: logical = true;

--}}

-- _Конструкторы, визуализаторы .--

-- _Обработчики событий шаблона. --

-- _Обработчики событий клеток шаблона. --

inobject private

  proc ПолеШаблона_ПриВыходе2_ 0.(Cell: TemplateCell; Index: Integer);
    if (Cell.Contents = 'НашеУчреждение'):
      ОбновитьФильтр;
    fi;
  end;

  func ПолеШаблона_ПриВыводе2_ 0.(Cell: TemplateCell; Value: Variant; Action: Template.OutputTypes; var Format: String): Variant;
    if (Cell.Contents = 'НашеУчреждение'):
      Result = inherited ПолеШаблона_ПриВыводе(Cell, Value, Action, Format);
    fi;
  end;

-- _Обработчики событий прочих объектов шаблона. --

-- _Обработчики событий картотеки. --

-- _Обработчики столбцов (полей) картотеки. --

-- _Дополнительные команды. --

-- _Вспомогательные методы. --

end
